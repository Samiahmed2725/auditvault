export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is optional with v4 but good to keep if needed, though v4 handles prefixes. Actually v4 handles it. But keeping it doesn't hurt usually, or I can remove. Safe to keep.
    },
}
